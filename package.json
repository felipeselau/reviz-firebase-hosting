{
  "name": "firebase-hosting-app-links",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "firebase emulators:start --only hosting",
    "deploy": "firebase deploy --only hosting"
  },
  "devDependencies": {
    "firebase-tools": "^13.0.0"
  }
}
